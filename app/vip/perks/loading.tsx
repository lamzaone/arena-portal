import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Individual VIP perks" title="Loading VIP perks" description="Preparing Token offers and the active perk roster." layout="catalogue" />;
}
