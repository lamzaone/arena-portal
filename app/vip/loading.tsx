import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="TAPPED.RO membership" title="Choose your advantage." description="Every current VIPCore benefit configured for ARENA.TAPPED.RO. Your current VIP status is checked against the game database after Steam login." layout="catalogue" />;
}
