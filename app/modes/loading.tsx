import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="ARENA.TAPPED.RO playbook" title="Modes made for the match." description="Browse every supported Arena round and every Custom Duel option in the current server configuration." layout="account" />;
}
