import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Player economy" title="Marketplace" description="Buy a specific cosmetic, case, or group membership item directly with Tokens." layout="catalogue" />;
}
