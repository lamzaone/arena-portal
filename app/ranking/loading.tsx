import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="TAPPED.RO leaderboard" title="Every point has a place." description="The top ARENA.TAPPED.RO players, ordered by K4 LevelRanks points. Each displayed rank is derived from the current K4 rank ladder." layout="table" />;
}
