import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Player economy" title="Trades" description="Find another player, compare both inventories, and build a secure item or Token offer." layout="trade" />;
}
