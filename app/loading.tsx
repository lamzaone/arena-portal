import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      eyebrow="TAPPED.RO Counter-Strike community"
      title="1v1's with a pulse."
      description="ARENA.TAPPED.RO is built for players who want every round to matter: instant arena fights, custom duels, rewarding ranks, and a loadout that is yours."
      layout="status"
    />
  );
}
