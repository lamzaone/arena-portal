import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Player economy" title="Crates" description="Open an owned crate or capsule to receive one random item. Crates never require a key." layout="catalogue" />;
}
