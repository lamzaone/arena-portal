import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Token rewards" title="Redeem" description="Claim a server or community reward code once, from anywhere." layout="form" />;
}
