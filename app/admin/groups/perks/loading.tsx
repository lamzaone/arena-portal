import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Staff panel" title="VIP perks" description="Preparing definitions, player and group assignments, and active grants." layout="form" staff staffSection="groups" />;
}
