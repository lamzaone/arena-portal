import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="VIP feature control" title="Loading VIP perk management" description="Preparing definitions, player and group assignments, and active grants." layout="form" staff staffSection="groups" />;
}
