import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Reward campaigns" title="Redeem codes" description="Build a reward once, choose its global claim limit, and let every player claim that code only once from the server or portal." layout="form" staff staffSection="redeem" />;
}
