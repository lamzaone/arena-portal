import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Player economy" title="Inventory" description="Manage the cosmetic items attached to your Token account, then equip eligible owned instances to your server loadout." layout="inventory" />;
}
