import { RouteLoading } from "@/components/ui/route-loading";

export default function Loading() {
  return <RouteLoading eyebrow="Player support" title="Tickets" description="Open a private ticket for a player report, an admin report, a bug, account help, or a VIP purchase request." layout="form" />;
}
